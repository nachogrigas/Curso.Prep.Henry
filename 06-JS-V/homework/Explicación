// Prototype es aquella clase que continen más que nada todos los métodos posibles a realizar para un clase especifica.evita el codigo no escalable
//Constructors es aquella funcion que crea un tipo con propiedades especificas permitiendo crear modelos del mismo de forma mas facil
